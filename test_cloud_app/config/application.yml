server:
  port: 8080

spring:
  profiles:
    active: default

# 自定义配置
app:
  name: Spring-Python-Test
  version: 1.0.0
  debug: true
  cache:
    max-size: 500
    ttl: 180

# 数据库配置示例
database:
  url: jdbc:mysql://localhost:3306/test
  username: admin
  password: secret
  pool-size: 10
