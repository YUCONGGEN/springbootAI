# 安全政策

## 支持范围

安全修复只发布到最新稳定版本。历史版本可能不再回补；升级前请阅读 [CHANGELOG.md](CHANGELOG.md) 的兼容性提醒。

## 如何报告漏洞

请不要在公开 Issue 中提交可利用细节、真实密钥、客户数据或攻击脚本。优先使用 GitHub 仓库的 **Security -> Report a vulnerability** 私密报告入口；如果仓库尚未开启私密报告，请联系 `1516933915@qq.com`，主题写明 `SpringBootAI security report`。

报告至少包含：

1. 受影响版本、Python 版本和操作系统。
2. 受影响模块、最小复现步骤和实际影响。
3. 是否需要认证、特定配置或外部服务。
4. 建议修复方式；如无建议可以省略。

维护者会先确认收到报告，再评估影响、准备修复和发布公告。在修复版本发布前，请不要公开利用方式。

## 使用者安全责任

- 密钥只从环境变量或密钥管理服务读取，不提交 `.env`、私钥和生产配置。
- 公网流量在专业反向代理/API Gateway/WAF 终止 TLS，并限制请求大小、速率和可信代理。
- AI/MCP Tool 采用白名单、参数校验、最小权限、执行超时和危险操作人工审批。
- LangGraph checkpoint 数据库禁止不可信写入，保持严格 msgpack 白名单和 `pickle_fallback=False`。
- Seata TCC 回调使用 TLS、共享 token/服务身份、host 白名单，并实现幂等、防空回滚和防悬挂。
- 部署前运行仓库 CI、安全扫描和目标环境故障测试；测试通过不替代组织自己的威胁建模和合规审查。
