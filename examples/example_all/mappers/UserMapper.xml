<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="example_all.mappers.UserMapper">
    <!--
        XML Mapper 文件 (MySQL 版)
        演示 @Mapper 接口与 XML 文件混合使用的模式。
    -->

    <resultMap id="UserResultMap" type="User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="created_at" property="created_at"/>
        <result column="updated_at" property="updated_at"/>
    </resultMap>

    <sql id="AllColumns">
        id, username, email, phone, created_at, updated_at
    </sql>

    <select id="find_all_xml" resultMap="UserResultMap">
        SELECT <include refid="AllColumns"/> FROM users
    </select>

    <select id="find_by_id_xml" resultMap="UserResultMap">
        SELECT <include refid="AllColumns"/> FROM users WHERE id = #{id}
    </select>

    <select id="search_users" resultMap="UserResultMap">
        SELECT <include refid="AllColumns"/> FROM users
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
            <if test="start_date != null">
                AND created_at &gt;= #{start_date}
            </if>
            <if test="end_date != null">
                AND created_at &lt;= #{end_date}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="find_by_ids" resultMap="UserResultMap">
        SELECT <include refid="AllColumns"/> FROM users WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="find_pagination" resultMap="UserResultMap">
        SELECT <include refid="AllColumns"/> FROM users
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="count_users" resultType="int">
        SELECT COUNT(*) FROM users
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
        </where>
    </select>

    <insert id="insert_xml" parameterType="User">
        INSERT INTO users (username, email, phone, created_at, updated_at)
        VALUES (#{username}, #{email}, #{phone}, NOW(), NOW())
    </insert>

    <insert id="batch_insert" parameterType="User">
        INSERT INTO users (username, email, phone, created_at, updated_at) VALUES
        <foreach collection="users" item="user" separator=",">
            (#{user.username}, #{user.email}, #{user.phone}, NOW(), NOW())
        </foreach>
    </insert>

    <update id="update_xml" parameterType="User">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete_xml">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <delete id="batch_delete">
        DELETE FROM users WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
