# ========================================
# SpringPy AI 模块可选依赖（精确锁定版本）
# ========================================
# AI 模块采用"可选依赖 + 降级"设计：
#   - 未安装本文件任何依赖时，AI 模块仍可用（降级原生 HTTP + FakeChatModel）
#   - 安装后自动启用 LangChain 生态适配（见 spring/ai/providers.py 的 _has_* 检测）
#
# 安装方式（按需）：
#   pip install -r requirements-ai.txt          # 全量
#   pip install langchain-openai==0.3.27        # 仅 OpenAI 适配
#
# 推荐 Python: 3.9+（最低 3.9，已测试 3.9.6）
# 所有版本号 == 已测试验证，禁止使用 >= 范围
# ========================================

# OpenAI 兼容模型适配（OpenAI / Azure / DeepSeek / Moonshot）
langchain-openai==0.3.27

# LangChain 核心（langchain-openai 的依赖，显式锁定）
langchain-core==0.3.51

# Ollama 本地模型适配（Llama3 / Qwen / Phi3 等）
langchain-community==0.3.24

# 向量相似度计算（可选，未安装时用内置 math.sqrt 实现）
numpy==2.0.2
